export  enum ObjectState {
  Unchanged = 0,
  Added,
  Modified,
  Deleted
}

export  enum EntityState {
    New = 0,
    Inserted
}

export enum ContractType {
  MGStatic = 0,
  MGDynamic,
  NonMG
}
